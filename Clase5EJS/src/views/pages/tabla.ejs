<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
  <h2>Productos Guardados</h2>

  <table class="table table-striped table-dark">
    <tr>
      <th>Nombre</th>
      <th>Precio</th> 
      <th>Thumbnail</th>
    </tr>
    <% for(var i=0; i<customerFile.length; i++) {%>
    <tr>
      <td><%= customerFile[i].Nombre %></td>
      <td><%= customerFile[i].Precio %></td> 
      <td><img src=<%= customerFile[i].url %> class="img-fluid" alt="Responsive image"></img></td>
    </tr>
    <% } %>
  </table>
  
  <form method="get" action="/">
      <button type="submit" class="btn btn-primary">Volver al Formulario</button>
  </form>
</body>
</html>